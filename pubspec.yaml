name: settings_screen_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3

  flutter_settings_screens: ^0.3.0-null-safety

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true